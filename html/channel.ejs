<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>YouTube</title>
    <link rel="icon" type="image/png" href="/assets/img/youtube.png" />
    <link rel="stylesheet" href="../css/header.css">
    <link rel="stylesheet" href="../css/nav.css">
    <link rel="stylesheet" href="../css/channel.css">

    <link href="https://fonts.googleapis.com/css2?family=Roboto&display=swap" rel="stylesheet">
</head>
<body>
    <!-- header : 홈 화면 상단바 -->
    <%- include('partials/header') %>

    <!-- nav : 홈 화면 네비게이션 -->
    <%- include('partials/nav') %>

    <!-- main : 홈 화면 블록 -->
    <main>
        <section class="channel-header">
            <img class="channel-banner" src="<%= channelInfo.channel_banner %>" alt="<%= channelInfo.channel_name %>">
            <!-- <img class="channel-banner" src="../assets/img/channelbanner.png" alt="channel"> -->

            <div class="channel-info">
                <div class="channel-left">
                    <!-- <img  class="channel-profile" src="../assets/img/MarcusLevin.svg" alt="Marcus Levin"> -->
                    <img class="channel-profile" src="<%= channelInfo.channel_profile %>" alt="<%= channelInfo.channel_name %>">
                    <div class="channel-text">
                        <!-- <h2 class="channel-owner">Marcus Levin</h2> -->
                        <!-- <p class="subscriber-num">1.2M subscribers</p> -->
                        <h2 class="channel-owner"><%= channelInfo.channel_name %></h2>
                        <p class="channel-subscription">구독자 <%= channelInfo.subscribers.toLocaleString() %>명</p>
                    </div>
                </div>
                <button class="subscribe-btn">SUBSCRIBE</button>
            </div>
            
            <nav class="channel-nav">
                <div class="nav-scroll-wrapper">
                    <!-- 왼쪽 버튼 -->
                    <button class="scroll-tab-btn scroll-tab-left">
                        <img src="/assets/icons/Top-Menu.svg" alt="←">
                    </button>
                
                    <!-- 스크롤 가능한 탭 컨테이너 -->
                    <div class="nav-links-scrollable">
                        <div class="nav-links">
                        <a href="#" class="active">HOME</a>
                        <a href="#">VIDEOS</a>
                        <a href="#">PLAYLIST</a>
                        <a href="#">COMMUNITY</a>
                        <a href="#">CHANNELS</a>
                        <a href="#">ABOUT</a>
                        <img src="../assets/icons/search.svg">
                        </div>
                    </div>
                
                    <!-- 오른쪽 버튼 -->
                    <button class="scroll-tab-btn scroll-tab-right">
                        <img src="/assets/icons/Top-Menu.svg" alt="→">
                    </button>
                </div>
            </nav>
        </section>

        <section class="channel-featured-video">
            <div class="video-thumbnail">
                <!-- 첫 번째 영상만 표시 -->
                <video controls width="640" height="360">
                    <source src="https://storage.googleapis.com/youtube-clone-video/<%= featuredVideo.id %>.mp4" type="video/mp4">
                    <!-- <source src="https://storage.googleapis.com/youtube-clone-video/<%= channelVideoList[0].id %>.mp4" type="video/mp4"> -->
                    Your browser does not support the video tag.
                </video>
            </div>
        
            <div class="featured-vid-info">
                <h3 class="featured-vid-title"><%= featuredVideo.title %></h3>
                <p>
                    <span class="featured-vid-views"><%= featuredVideo.views.toLocaleString() %> views • </span>
                    <span class="featured-vid-date"><%= getRelativeTime(featuredVideo.created_dt) %></span>
                </p>
                <p class="featured-vid-description">
                    <!-- description이 있다면 description, 없다면 tags 나열해도 괜찮 -->
                    <% if (featuredVideo.description) { %>
                        <%= featuredVideo.description %>
                    <% } else { %>
                        <%= featuredVideo.tags.join(' • ') %>
                    <% } %>
                </p>
            </div>
        </section>

        <section class="channel-video-listall">
            <% 
            const chunkSize = 6;
            for (let i = 0; i < channelVideoList.length; i += chunkSize) { 
                const chunk = channelVideoList.slice(i, i + chunkSize); 
            %>
                <div class="channel-video-list">
                    <div class="section-header">
                        <h2>Videos</h2>
                        <button class="play-all">
                            <p>▶ PLAY ALL</p>
                        </button>
                    </div>
                    <div class="scroll-wrapper">
                        <!-- 왼쪽 버튼 -->
                        <button class="scroll-left">
                            <img src="../assets/icons/Top-Menu.svg" alt="이전 버튼" />
                        </button>

                        <div class="video-list scrollable-list">
                            <% chunk.forEach(video => { %>
                                <article class="video-card">
                                    <a href="/video?id=<%= video.id %>">
                                        <figure class="video-figure">
                                            <img class="thumbnail" src="<%= video.thumbnail %>" alt="썸네일">
                                            
                                            <video class="preview" muted loop preload="none">
                                                <source src="https://storage.googleapis.com/youtube-clone-video/<%= video.id %>.mp4" type="video/mp4">
                                                Your browser does not support the video tag.
                                            </video>
                                        </figure>
                                        <figcaption class="video-info">
                                            <div class="video-description">
                                                <p class="video-title"><%= video.title %></p>
                                                <p>
                                                    <span class="channel-name"><%= channelInfo.channel_name %></span><br>
                                                    <span class="view"><%= formatViews(video.views) %> Views • </span>
                                                    <span class="update-date"><%= getRelativeTime(video.created_dt) %></span>
                                                </p>
                                            </div>
                                        </figcaption>
                                    </a>
                                </article>
                            <% }); %>
                        </div>

                        <!-- 오른쪽 버튼 -->
                        <button class="scroll-right">
                            <img src="../assets/icons/Top-Menu.svg" alt="다음 버튼" />
                        </button>
                    </div>
                </div>
            <% } %>
        </section>
    </main>
    <script src="../js/button.js"></script>
    <script src="/js/thumbnail-play.js"></script>
    <script src="../js/channel_search.js"></script>
    <script src="../js/channel-scroll.js"></script>
    <script src="/js/channel-nav-scroll.js"></script>
</body>
</html>